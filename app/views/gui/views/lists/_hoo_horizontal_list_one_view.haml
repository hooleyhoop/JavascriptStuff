- push( hoo_horizontal_list_one_view )

-# http://0.0.0.0:3000/widgets/horizontalList1
%ul[_]
	- _.content.each do |item|
		%li{ :style=>"display:inline; overflow: auto; display: inline-block;" }
			= render :partial=>_.cell.partial, :object=>item, :locals=>{ :_ => _.cell }
