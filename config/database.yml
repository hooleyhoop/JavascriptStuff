development:
    adapter: mysql
    encoding: utf8
    host: localhost    
    reconnect: false
    database: javascript_stuff_development
    pool: 5
    username: root
    password: NGPrague 
    sql_sock: /tmp/mysql.sock

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
    adapter: mysql
    encoding: utf8
    host: localhost    
    reconnect: false
    database: javascript_stuff_test
    pool: 5
    username: root
    password: NGPrague
    sql_sock: /tmp/mysql.sock

production:
    adapter: mysql
    encoding: utf8
    host: localhost    
    reconnect: false
    database: javascript_stuff_production
    pool: 5
    username: root
    password: NGPrague
    sql_sock: /tmp/mysql.sock
